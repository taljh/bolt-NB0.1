/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  eslint: {
    ignoreDuringBuilds: true,
  },
  images: { unoptimized: true },
  cache: false, // Temporarily disable caching to resolve the webpack issue
};

module.exports = nextConfig;